KEY CHANGES NEEDED FOR DIRECTIONAL ROUTING:

1. Add incoming_direction array: integer(rows, cols)
   - Tracks which direction (1-8) the signal came from for each neuron
   - Initialize to 0 (no incoming signal)
   - Direction 7 (Down) for input from inputter

2. Update synapse indexing from 3D to 4D:
   OLD: synapses(i, j, outgoing_dir)
   NEW: synapses(i, j, incoming_dir, outgoing_dir)

3. When signal propagates from (i,j) to (ni,nj):
   - Use incoming_direction(i,j) to select synapse group
   - Set incoming_direction(ni,nj) = opposite_direction(chosen_outgoing)

4. Direction opposites (for tracking where signal came from):
   1 (Up-Left) ↔ 8 (Down-Right)
   2 (Up) ↔ 7 (Down)
   3 (Up-Right) ↔ 6 (Down-Left)
   4 (Left) ↔ 5 (Right)

5. Update synapse_usage to 4D: (rows, cols, 8, 8)

6. For inputter: signals come "from above" = direction 7 (Down)
